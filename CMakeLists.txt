cmake_minimum_required(VERSION 3.16)
project(reverbbridge LANGUAGES CXX)
set(CMAKE_WINDOWS_EXPORT_ALL_SYMBOLS ON)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
add_library(reverbbridge SHARED reverb_bridge.cpp)
set_target_properties(reverbbridge PROPERTIES OUTPUT_NAME "reverbbridge")
